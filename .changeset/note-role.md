---
"@cronn/element-snapshot": minor
---

Add support for elements with `role="note"`

Snapshot elements with an explicit `note` role. The `note` role supports an accessible name from `aria-label`/`aria-labelledby`.
